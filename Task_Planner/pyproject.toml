[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "task-planner"
version = "0.3.0"
description = "Task Planner: contract-aware search for subgoal order, EE, and fixed-tool resource transitions"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.5",
]

[project.optional-dependencies]
test = [
    "pytest>=8",
]

[tool.setuptools.packages.find]
include = ["task_planner*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
